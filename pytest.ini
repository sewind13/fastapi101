# pytest.ini
[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths =
    tests/unit
    tests/integration
markers =
    unit: fast isolated tests
    integration: application and database integration tests
    postgres: integration tests that require TEST_DATABASE_URL
